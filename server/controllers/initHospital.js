const hospital = require('../models/hospital')

const qureyAllPatient = function* () {
  const hospitalName = this.request.body
  const hospitalInfo = {
    name: 'hospital01',
    password: 'passward'
  }
}

const qureyAllCase = function* () {
  const 
}
module.exports = {

  postDoctorAuth
}